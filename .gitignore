# Node (tools/xqf-decoder và các tool script khác)
node_modules/

# Nội dung có bản quyền — KHÔNG BAO GIỜ commit (xem CLAUDE.md mục Bản Quyền & Bảo Mật)
tools/xqf-decoder/fixtures/
storage/app/private/cotuong-sources/

# Laravel mặc định (áp dụng khi composer create-project chạy — Phase 1)
/vendor
/node_modules
.env
.env.backup
.phpunit.result.cache
docker-compose.override.yml
Homestead.json
Homestead.yaml
npm-debug.log
yarn-error.log
/.fleet
/.idea
/.vscode
/storage/*.key
/public/build
/public/storage

# Ảnh upload người dùng (Tin tức: thumbnail bài viết + ảnh chèn TinyMCE) — không commit, mỗi máy tự có.
/storage/app/public/posts
